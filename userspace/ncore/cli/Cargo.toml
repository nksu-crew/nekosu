[package]
name = "ncore"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
clap = { version = "4.6.1", features = ["derive"] }
kmod = { path = "../kmod" }
boot = { path = "../boot" }
modules = { path = "../modules" }
